# Optional overrides for the local stack.
# Prefer putting machine-specific config in config/local/*.yaml.
# Environment variables remain useful for executable paths and secrets.

LLAMA_SWAP_EXE=C:\development\tools\llama-swap\llama-swap.exe
LITELLM_MASTER_KEY=sk-local-dev
