# AUDia LLM Gateway — sample backend runtime overrides.
# Keep the checked-in file generic. Machine-specific pins, alternate upstreams,
# or host-only build variants belong in config/local/backend-runtime.private.yaml.
#
# defaults:
#   always: false
#
# profiles:
#   build-rocm-sample:
#     backend: rocm
#     configure_command: cmake -S . -B build -DLLAMA_BUILD_SERVER=ON -DGGML_HIPBLAS=ON -DCMAKE_BUILD_TYPE=Release
#     build_command: cmake --build build --config Release --parallel
#     binary_glob: build/bin/llama-server
#     library_glob: build/bin/*.so*
#     apt_packages:
#       - git
#       - cmake
#       - build-essential
#
# variants:
#   rocm-sample-preview:
#     profiles:
#       - build-rocm-sample
#     source_type: git
#     git_url: https://github.com/ggml-org/llama.cpp.git
#     git_ref: master
#     macro: llama-server-rocm-sample-preview
#     runtime_subdir: rocm/sample-preview
