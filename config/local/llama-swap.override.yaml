# Machine-local overrides only.
# Docker backend wrappers and model-path macros are synthesized from the
# runtime catalog when AUDIA_DOCKER=true, so keep this file empty unless you
# need a truly local custom override.
macros: {}
models: {}
groups: {}
