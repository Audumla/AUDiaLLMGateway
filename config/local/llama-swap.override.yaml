# AUDia LLM Gateway — sample llama-swap overrides.
# Keep tracked macros generic. Host-only executable paths and version-pinned
# aliases belong in config/local/llama-swap.private.yaml.
macros:
  model-path: "--model /app/models/gguf"
  mmproj-path: "--mmproj /app/models/gguf"
models: {}
groups: {}
